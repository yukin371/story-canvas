# 提交规则

> 最后更新: YYYY-MM-DD
> 状态: 当前有效提交治理规则

## 1. 目的

本文件用于统一仓库的提交信息格式、本地 hooks 和 CI 侧治理规则。

## 2. commit message 格式

推荐格式:

`<type>(<scope>): <description>`

允许的 `type`:

- `feat`
- `fix`
- `docs`
- `refactor`
- `test`
- `chore`
- `build`
- `ci`
- `perf`
- `revert`
- `style`

约束:

- description 简洁明确
- 不使用空泛描述
- 不把 issue 流水账写进 description
- 若仓库启用“适用规则”治理，提交说明与 PR 描述不应互相矛盾

## 3. 禁止的协作元信息

默认禁止:

- `Co-Authored-By:`
- `Co-authored-by:`
- `Pair-Programmed-By:`
- AI 工具签名或“Generated by ...”类尾注

## 4. 本地防线

- `commit-msg` hook 拦截非法提交信息
- `pre-commit` hook 拦截明显违规内容，并可提示本次改动的规则触点
- commit template 作为默认提示

## 5. CI 防线

- 扫描提交范围内的 commit messages
- 阻止 forbidden trailers 和非法提交格式
- 若启用 PR 描述治理，应与 PR body 校验一起作为合并前门禁
